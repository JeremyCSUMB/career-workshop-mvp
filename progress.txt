## Codebase Patterns
- Use `$app/environment` `browser` guard before accessing `localStorage` in Svelte components
- InterviewScreen phases are component-local `$state`, not in the interview store — persist them separately to `ws_*` localStorage keys
- Svelte 5 runes: use `$state()` for reactive local state, `$derived()` for computed, `$effect()` for side effects
- Typecheck with `npx svelte-check` (no tsconfig flag needed)
- Interview store in `src/lib/stores/interview.js` auto-persists on `.update()` calls but only for store-level keys
- The template conditionally renders phases with `{#if phase === 'notes'}` etc., so restoring `phase` directly skips earlier steps
- Session lifecycle handlers (`handleLeave`, `handleSwitchSession`, `handleChangeRoom`) live in `+page.svelte` — new `ws_*` keys must be added to `clearEphemeralKeys()` and `reset()` in `interview.js`
- There are 4 `data.ended` detection paths in `+page.svelte` — ephemeral cleanup must cover all of them
- `workshop-room` GET returns the full room blob — `submissions`, `aiFollowUps`, `capabilityProfiles` are all available at `data.room.*`
- `makeEmptyRoom` in `workshop-session.js` initializes the room shape — add new fields there for new rooms
- Old rooms may lack `capabilityProfiles` (plural) — `workshop-room.js` normalizes this on read
- Resume path must call `workshop-join` before `workshop-room` — join re-registers the student, room fetches full data (submissions, AI data)
- `aiFollowUps` is array of `{ questions, timestamp }`, `capabilityProfiles` is array of `{ summary, capabilities, studentName, round }` — match by round for profile, use last entry for followups
- Pass transient server data via props to child components rather than persisting to the interview store/localStorage
- Submissions use round labels like `round1-notes`, `round1-followup` — match with `round${currentRound}-notes` pattern; the `notes` field holds the text
- SvelteKit server load functions: use `cookies.get('session')` for cookie access, `throw redirect(302, url)` for redirects
- `jsonwebtoken` works as ES module import in SvelteKit server files — SvelteKit handles CJS interop
- User data from +page.server.js load is available via `let { data } = $props()` in Svelte 5 pages
- Shared cookie parser lives in `netlify/functions/lib/cookies.js` — use `parseCookies(event.headers.cookie)` in any Netlify function
- Room blobs have optional `studentEmails` field — old rooms may lack it, always initialize with `if (!room.studentEmails) room.studentEmails = {}`
- User profile blobs stored at `user:{email}` key — contain name, email, picture, authenticatedAt
- The auth-logout endpoint already exists at netlify/functions/auth-logout.js — POST clears session cookie with Max-Age=0
- Header CSS classes: `.ws-header` (centered, max-width 640px) for interview, `.ws-header--wide` (max-width 1080px) for dashboard
- Existing CSS design system: `.ws-btn`, `.ws-btn--secondary`, `.ws-btn--small`, `.ws-btn--danger`, `.ws-card`, `.ws-link-btn`
- Theme CSS vars: `--ci-accent`, `--ci-text`, `--ci-surface`, `--ci-border`, `--ci-focus-ring`
- UserProfileMenu component: uses `onLogout` callback prop (not `createEventDispatcher`), accepts `name`/`email`/`picture`/`onLogout` props
- Svelte 5 pattern: use callback props (`onLogout`) not `createEventDispatcher` — matches existing codebase convention
- InterviewScreen has two poll paths: storytellerPoll (5s, round advancement) and presencePoll (5s, interviewer only) — both call `updatePartnerPresence(room)` to track partner status
- Partner presence slot: match studentName against `room.students.student1`/`student2`, use the opposite slot's presence data
- Position profile menu in headers with `.ws-header__profile` class: `position:absolute; right:24px; top:50%; transform:translateY(-50%)`
- movedTo/movedFrom markers on room blobs: `${slot}_movedTo` / `${slot}_movedFrom` with `{ roomId, studentName, timestamp }` — detect moves by checking if studentName is no longer in room.students, then reading the marker
- Move detection in InterviewScreen: `checkForRoomMove(room)` runs in both poll paths — stops polls and calls `onMoved` callback when a move is detected
- Partner change ready flag: heartbeat accepts `readyForRound` param, stored in `presence[slot].readyForRound` — poll workshop-room to check partner's readyForRound
- New partner detection (existing student): compare `$interviewState.partnerName` against room poll data — if partner name changed and `movedFrom` marker exists, trigger partner-change screen
- PartnerChangeScreen supports `variant` prop: 'moved' (US-012, moved student) vs 'existing' (US-013, existing student) — different messages, same Ready/timeout logic

---

## 2026-04-15 - US-002
- Added presence normalization to `workshop-room.js` — old rooms without `presence` get default `{ student1: { online: false, lastSeen: null }, student2: ... }`
- Added per-room `presence` object to `workshop-pulse.js` response with server-side computed `online` boolean based on 30s threshold
- Files changed: `netlify/functions/workshop-room.js`, `netlify/functions/workshop-pulse.js`
- **Learnings for future iterations:**
  - Pulse endpoint computes `online` server-side using `Date.now() - new Date(lastSeen).getTime() <= 30000` — clients should use the boolean directly, not recompute
  - The presence object in pulse has the same shape as in the room blob: `{ student1: { online, lastSeen }, student2: { online, lastSeen } }`
  - Old rooms without presence field are normalized in both endpoints — no client-side null checks needed
---

## 2026-04-15 - US-001
- Added `presence` field to `makeEmptyRoom` in `workshop-session.js` with `student1`/`student2` sub-objects (`online: false`, `lastSeen: null`)
- Updated `workshop-heartbeat.js` to accept `studentName`, read the room blob, identify the student's slot, and write `online: true` + ISO `lastSeen` timestamp
- Heartbeat gracefully initializes `presence` if missing on old rooms
- Added `presence_timeout: 30000` to `WORKSHOP_CONFIG` in `src/lib/config.js`
- Updated client heartbeat call in `+page.svelte` to send `studentName`
- Files changed: `netlify/functions/workshop-session.js`, `netlify/functions/workshop-heartbeat.js`, `src/lib/config.js`, `src/routes/interview/+page.svelte`
- **Learnings for future iterations:**
  - The heartbeat previously only wrote to a separate `heartbeat:{sessionId}:{roomId}` blob — US-001 adds a read-modify-write on the room blob for presence, which introduces a potential race with join/leave. Keep heartbeat writes minimal.
  - Student slot identification: `room.students.student1` and `room.students.student2` hold the student name strings directly
  - The client heartbeat is in `startHeartbeat()` around line 72 of `+page.svelte` — it uses `$interviewState` for sessionId/roomId/studentName
---

## 2026-04-15 - US-003
- Created `src/lib/components/Toast.svelte` — reusable toast notification component
- Accepts `message` (string), `duration` (ms, default 5000), `type` ('info'/'success'/'warning'), and `visible` ($bindable boolean) props
- Fixed position bottom-right, slide-in animation on appear, fade-out on dismiss
- Uses CSS variables for theming with dark mode support via `:global(.dark)` selectors
- Auto-dismisses after specified duration
- Files changed: `src/lib/components/Toast.svelte`
- **Learnings for future iterations:**
  - Toast uses `$bindable(visible)` so parent can two-way bind: `<Toast bind:visible={showToast} message="..." />`
  - To show a toast: set the bound `visible` to `true`; it auto-resets to `false` on dismiss
  - Dark mode overrides for non-themed colors (success green, warning amber) use `:global(.dark) .ws-toast--type` pattern
---

## 2026-04-15 - US-004
- Added partner presence tracking to InterviewScreen with `partnerOnline`/`partnerPreviousOnline` state
- Added `updatePartnerPresence()` helper that identifies partner's slot and computes online status from lastSeen + 30s threshold
- Interviewer gets a dedicated `presencePollInterval` (5s); storyteller piggybacks on existing `storytellerPollInterval`
- Yellow/amber disconnection banner shown between round header and prompt card when partner is offline
- Banner auto-dismisses when partner comes back online (reactive via `partnerOnline` state)
- `partnerPreviousOnline` starts as `null` to prevent banner on initial page load
- Added `.ws-partner-disconnected-banner` styles to `styles.css` with dark mode support
- Files changed: `src/lib/components/interview/InterviewScreen.svelte`, `styles.css`
- **Learnings for future iterations:**
  - InterviewScreen has two poll paths: storyteller poll (round advancement) and interviewer presence poll — both now update partner presence
  - Partner slot identification: match `$interviewState.studentName` against `room.students.student1`/`student2`, then use the other slot's presence
  - The `partnerPreviousOnline = null` pattern prevents showing banners/toasts on initial load — use it for US-005 transition detection
---

## 2026-04-15 - US-005
- Added partner reconnection toast: when partner transitions from offline→online, shows "Your partner is back online!" success toast
- Imported Toast component from US-003, added `showReconnectToast` state variable
- Added `$effect` that watches `partnerPreviousOnline === false && partnerOnline === true` to detect the transition
- Toast auto-dismisses after 5 seconds, does not show on initial page load (guarded by `partnerPreviousOnline` starting as `null`)
- Files changed: `src/lib/components/interview/InterviewScreen.svelte`
- **Learnings for future iterations:**
  - The `$effect` for transition detection is clean: just watch the two reactive state vars — Svelte 5 re-runs the effect when either changes
  - Toast `bind:visible` pattern makes it easy to trigger from any `$effect` — just set the bound var to `true`
---

## 2026-04-15 - US-006
- Added `navigator.sendBeacon` autosave on `beforeunload` event — saves current notes/followup text to server before tab closes
- Uses `Blob` with `application/json` content type to match `workshop-submit` endpoint expectations
- Updated hydration logic to prefer server submissions over localStorage on reconnect — server data reflects the latest sendBeacon/debounced save
- Followup text is only beacon-saved when phase is 'followup' (avoids sending stale data from previous phases)
- Files changed: `src/lib/components/interview/InterviewScreen.svelte`, `prd.json`
- **Learnings for future iterations:**
  - `navigator.sendBeacon` requires a `Blob` with explicit content type for JSON payloads — `new Blob([JSON.stringify(data)], { type: 'application/json' })`
  - The `workshop-submit` endpoint's upsert pattern (findIndex by studentName + round) handles beacon saves seamlessly — no server changes needed
  - Server-priority hydration: when `resumeRoomData` exists, always use server submission data over localStorage, since sendBeacon may have saved data that localStorage missed
---

## 2026-04-15 - US-007
- Added welcome-back toast on reconnection: "Welcome back! You're in Round [X] as [Interviewer/Storyteller]."
- Added `rejoined` state in `+page.svelte`, set to `true` when `workshop-join` returns `rejoined: true` (both resume flow and handleJoinRoom)
- Passed `rejoined` prop to InterviewScreen, which shows an info Toast on mount when true
- Toast only fires once per reconnection (effect depends on `rejoined` and `role`, both set once)
- Files changed: `src/routes/interview/+page.svelte`, `src/lib/components/interview/InterviewScreen.svelte`, `prd.json`
- **Learnings for future iterations:**
  - `workshop-join` returns `rejoined: true` when a student re-registers in a room they were already in — use this to distinguish fresh joins from reconnections
  - Two Toast instances can coexist in the template — they manage their own visibility independently via `bind:visible`
  - The resume flow in `+page.svelte` onMount has two rejoin paths: the localStorage resume (lines ~335-388) and handleJoinRoom with `data.rejoined` — both need the `rejoined` flag
---

## 2026-04-15 - US-008
- Added presence indicators (green/red dots) next to student names in dashboard room cards
- Green dot for online students, red dot for offline students with gray "offline since [time]" timestamp below
- Added `students` object to workshop-pulse response so dashboard can map student slots to presence data
- Updated MonitorScreen to pass `_presence` and `_students` through to RoomCard via both pulse quick-update and full refresh enrichment
- Students with no presence data (empty rooms, old rooms) show no dot — handled by null-checking presence
- Files changed: `netlify/functions/workshop-pulse.js`, `src/lib/components/dashboard/MonitorScreen.svelte`, `src/lib/components/dashboard/RoomCard.svelte`, `styles.css`
- **Learnings for future iterations:**
  - Pulse endpoint returns `presence` keyed by `student1`/`student2` — need the `students` object (also keyed by slot) to map names to presence data
  - RoomCard gets presence via `room._presence` and student slots via `room._students` — both set in `enrichRoom()` and pulse quick-update
  - `getPresenceForStudent(name)` maps a student name to their presence slot by matching against `students.student1`/`student2`
  - Presence updates reactively on the 2s pulse cycle without needing a full refresh — the quick-update path copies presence data
---

## 2026-04-15 - US-009
- Created `netlify/functions/workshop-move-student.js` — POST endpoint that atomically moves a student from one room to another
- Validates session exists, both rooms exist, student is in source room, and target room has an available slot
- Removes student from source room: clears student slot, email, and resets presence
- Adds student to target room's first available slot with presence set to online
- Sets `student1_movedTo`/`student2_movedTo` on source room for stale client write rejection
- Sets `student1_movedFrom`/`student2_movedFrom` on target room for client-side move detection
- Preserves submissions in source room (no deletion)
- Resets target room's `currentRound` to match source room's round, clears `roundStartTime`
- Returns 409 if target room is full, 200 with `{ success: true, newRoom: targetRoomData }` on success
- Files changed: `netlify/functions/workshop-move-student.js`
- **Learnings for future iterations:**
  - movedTo/movedFrom fields are stored as `${slot}_movedTo` / `${slot}_movedFrom` on the room object (e.g., `student1_movedTo`)
  - Each marker contains `{ roomId, studentName, timestamp }` for client detection
  - The endpoint writes both rooms sequentially — no transaction support in Netlify Blobs, so there's a small window where source is updated but target isn't
  - Target room's currentRound is reset to sourceRoom.currentRound so the pair restarts at the moved student's progress level
---

## 2026-04-15 - US-010
- Added "Move Student" button to RoomCard that appears when one student is offline >2 minutes and the other is online
- Created MoveStudentModal component with two-step flow: room selection → confirmation
- Modal lists available target rooms (0 or 1 students), shows room number and current student name
- On confirmation, POSTs to workshop-move-student endpoint; on success triggers full dashboard refresh
- Handles 409 (room full) by showing error and returning to room selection step
- Added CSS styles for move room list, room options, and error messages
- Files changed: `src/lib/components/dashboard/RoomCard.svelte`, `src/lib/components/dashboard/MoveStudentModal.svelte` (new), `src/lib/components/dashboard/MonitorScreen.svelte`, `styles.css`
- **Learnings for future iterations:**
  - `moveEligibleStudent` is computed from presence data in RoomCard using a 2-minute threshold — re-evaluates each second via `tick` dependency
  - MoveStudentModal receives the full `rooms` array from MonitorScreen to compute available targets client-side
  - The move modal follows the same NudgeModal pattern: overlay click to close, Escape key, focus trapping, previous focus restoration
  - `onMoveStudent(roomId, studentName)` callback passes both the source room ID and the online student's name to the parent
---

## 2026-04-15 - US-011
- Added `checkForRoomMove(room)` async helper to InterviewScreen that detects when the student has been moved out of their room
- Detection: checks if studentName is no longer in `room.students`, then reads `student1_movedTo`/`student2_movedTo` markers for the new roomId
- On detection: fetches new room data via `workshop-room`, stops polls, calls `onMoved({ newRoomId, newRoom })` callback
- Integrated move check into both poll paths (storyteller poll and interviewer presence poll) — runs before presence/round updates
- Added `handleMoved` handler in `+page.svelte`: stops all polling, clears ephemeral keys, updates store with new roomId/round/students, restarts heartbeat, transitions to `partner-change` screen
- Added placeholder `partner-change` screen in template (US-012 will implement the full component)
- Files changed: `src/lib/components/interview/InterviewScreen.svelte`, `src/routes/interview/+page.svelte`, `prd.json`
- **Learnings for future iterations:**
  - Move detection must run before presence updates — if the student is gone from the room, there's no partner slot to check
  - The movedTo marker's `studentName` field lets you match which marker belongs to the moved student (important when slot could be either student1 or student2)
  - `handleMoved` in +page.svelte clears role/partnerName to null/empty since the new room may have different partner — US-012's partner-change screen will re-determine roles
  - The `partner-change` screen state is a new screen value that doesn't appear in the `navItems` derived — bottom nav defaults to 'home' for this screen
---

## 2026-04-15 - US-012
- Created `PartnerChangeScreen.svelte` — transition screen shown when a student is moved to a new room
- Shows message with round number and new partner's name, plus a "Ready" button
- Clicking Ready sends heartbeat with `readyForRound` parameter to write ready flag to room blob's presence
- Polls workshop-room every 2s to check partner's `presence[slot].readyForRound`
- Auto-transitions to interview after both students click Ready, or 10 seconds elapsed
- Extended `workshop-heartbeat.js` to accept optional `readyForRound` field, stored in `presence[slot].readyForRound`
- Replaced placeholder partner-change screen in `+page.svelte` with PartnerChangeScreen component
- `handlePartnerChangeReady` calls `startInterview()` which re-determines roles by alphabetical sort of the new pair
- Files changed: `netlify/functions/workshop-heartbeat.js`, `src/lib/components/interview/PartnerChangeScreen.svelte` (new), `src/routes/interview/+page.svelte`
- **Learnings for future iterations:**
  - Ready flag is stored as `presence[slot].readyForRound = <round number>` — check equality with current round to determine readiness
  - The 10s auto-transition ensures no permanent blocking if partner doesn't click Ready
  - `startInterview()` in +page.svelte handles role re-determination — no need to duplicate that logic in the partner change screen
  - Partner name derived from `students.filter(n => n !== studentName)` since store already has the new room's students from `handleMoved`
---

## 2026-04-15 - US-013
- Added `checkForNewPartner(room)` to InterviewScreen — detects when partner name changes and `movedFrom` marker is present on the new student's slot
- Integrated new partner detection into both poll paths (storyteller and interviewer presence), running after move-out check but before presence updates
- Added `onNewPartner` callback prop to InterviewScreen, handled by `handleNewPartner` in `+page.svelte`
- `handleNewPartner` stops polls, clears ephemeral keys, updates store with refreshed students/round, transitions to `partner-change-existing` screen
- Extended PartnerChangeScreen with `variant` prop: 'existing' shows "A new partner has joined your room!" message; 'moved' (default) shows original message
- Both variants share the same Ready button + 10s auto-transition logic
- In-progress submissions are preserved on the server (not deleted) — only ephemeral localStorage keys are cleared
- Files changed: `src/lib/components/interview/InterviewScreen.svelte`, `src/routes/interview/+page.svelte`, `src/lib/components/interview/PartnerChangeScreen.svelte`, `prd.json`
- **Learnings for future iterations:**
  - New partner detection must run after move-out check — if the student was moved out, there's no point checking for new partners in the old room
  - The `variant` prop pattern on PartnerChangeScreen avoids duplicating the Ready/timeout/poll logic for the two perspectives
  - `partner-change-existing` is a separate screen value from `partner-change` so `+page.svelte` can pass the correct variant
  - Ephemeral keys are cleared on partner change so the new round starts fresh — server submissions from the interrupted round remain intact
---
